# 0.15
* Transparently detect and decode the fugu binary symbol-table wire protocol
  (raw or tamp-compressed) alongside text line protocol, per source device.

# 0.13
* Use python venv

# 0.11
* Add exception handling to rx thread